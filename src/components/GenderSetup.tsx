import { Text, View } from 'react-native'

const GenderSetup = () => {
	return (
		<View style={{ flex: 1, flexGrow: 1, justifyContent: 'center', alignItems: 'center' }}>
			<Text>GenderSetup</Text>
		</View>
	)
}

export default GenderSetup
