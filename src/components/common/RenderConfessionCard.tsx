import React from 'react';
import PostCard from '../ui/PostCard';

const RenderConfessionCard = ({ item, index }: any) => <PostCard item={item} index={index} />;

export default RenderConfessionCard;
