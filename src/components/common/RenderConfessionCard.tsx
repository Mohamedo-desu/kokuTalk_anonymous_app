import React from 'react';
import PostCard from '../ui/PostCard';

const RenderConfessionCard = ({ item, index, themeColors, router }: any) => (
  <PostCard
    item={item}
    index={index}
    themeColors={themeColors}
    themeColors={themeColors}
    router={router}
  />
);

export default RenderConfessionCard;
